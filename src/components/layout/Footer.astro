---
import { site } from '@/lib/site';

const year = new Date().getFullYear();
---

<footer class="relative border-t border-border mt-24">
  <div class="container-x py-14 grid grid-cols-1 md:grid-cols-3 gap-12">
    <div>
      <div class="font-display text-lg">{site.name}</div>
      <p class="mt-3 text-sm text-text-muted max-w-xs leading-relaxed">
        {site.description}
      </p>
    </div>

    <div>
      <h4 class="eyebrow mb-3">Contact</h4>
      <a
        href={`mailto:${site.email}`}
        class="text-sm text-text hover:text-accent transition-colors link-underline"
      >
        {site.email}
      </a>
      <div class="mt-2 text-sm text-text-muted">{site.location}</div>
    </div>

    <div>
      <h4 class="eyebrow mb-3">Elsewhere</h4>
      <ul class="space-y-1.5 text-sm">
        <li>
          <a
            href={site.social.github}
            class="text-text-muted hover:text-text transition-colors"
            >GitHub</a
          >
        </li>
        <li>
          <a
            href={site.social.linkedin}
            class="text-text-muted hover:text-text transition-colors"
            >LinkedIn</a
          >
        </li>
      </ul>
    </div>
  </div>

  <div class="container-x py-6 border-t border-border flex items-center justify-between text-xs font-mono text-text-dim">
    <span>© {year} {site.name}</span>
    <span>Crafted with care · Built on Astro</span>
  </div>
</footer>
